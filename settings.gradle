pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "MultiRepoProject"
include ':app'
include ':ReusableComponents:uicomponents'
include ':ShowHidePasswordView:ShowHidePasswordView'

//project(':uicomponents').prjectDir = File('ReusableComponents/uicomponents')
//project(':ShowHidePasswordView').prjectDir = File('ShowHidePasswordView/ShowHidePasswordView')